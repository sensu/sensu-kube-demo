{
  "checks": {
    "network-device-health": {
      "command": "/usr/lib64/nagios/plugins/check_http -N -H localhost -p 8080 -u /healthz",
      "subscribers": ["network-device"],
      "interval": 5,
      "handler": "slack",
      "contacts": ["networkteam"]
    }
  }
}
